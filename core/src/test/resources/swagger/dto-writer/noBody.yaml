openapi: 3.0.0
info:
  title: No body API
  version: 1.0.0
servers:
  - url: http://localhost:5000
paths:
  /example:
    get:
      summary: Example with no body params
      parameters:
        - in: query
          name: aQueryParam
          schema:
            type: string
        - in: header
          name: aHeaderParam
          schema:
            type: string
      responses:
        '200':
          description: OK
