CREATE TABLE Parent(
  id1 bigint,
  id2 bigint,
  primary key (id1, id2)
);

CREATE TABLE Child(
  id bigint generated by default as identity,
  pid1 bigint not null,
  pid2 bigint not null
);

ALTER TABLE Child add constraint compositeKey
    foreign key (pid1, pid2) references Parent(id1, id2);
