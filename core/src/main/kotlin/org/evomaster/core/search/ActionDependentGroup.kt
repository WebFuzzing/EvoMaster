package org.evomaster.core.search

class ActionDependentGroup(children: MutableList<out Action>) : ActionTree(children) {
    override fun copyContent(): StructuralElement {
        TODO("Not yet implemented")
    }
}